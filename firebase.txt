// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyCYDBIo25bdS94qP_OP2Z9zxmE_VBdk4KU",
  authDomain: "real-estate-vue.firebaseapp.com",
  projectId: "real-estate-vue",
  storageBucket: "real-estate-vue.appspot.com",
  messagingSenderId: "410152438240",
  appId: "1:410152438240:web:816cd83066f1695a520385",
  measurementId: "G-JGPCXRMT27"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);


for deploying the app with firebase! in terminal.

//login to firebase
firebase login

//initiate firebase
firebase init

// deploying the app
firebase deploy


-- login en regisster component with styling<template>
<div class="h-screen flex flex-col justify-center 
  items-center bg-gray-100 text-[#00AAA2]">
  <div class="pb-9 md:pb-28">
    <img src="@/assets/logos/logo-horizontal.svg" 
      alt="logo" 
      class="w-48 md:w-96" 
    />
  </div>
  <form class="shadow shadow-[#475E6C] rounded-md md:rounded-2xl p-8 md:p-20">
    <legend class="text-[#293439] pb-4 uppercase text-lg md:text-4xl font-semibold">Register</legend>
    <div class="flex flex-col">
      <label class="md:text-2xl" for="username">Username</label>
      <input 
        type="text" 
        name="username" 
        autocomplete="true"
        v-model="user.username"
        id="username" 
        placeholder="username" 
        class="login-input"
        required
      />
    </div>
    <div class="flex flex-col">
      <label class="md:text-2xl" for="password">Password</label>
      <input 
        type="password" 
        name="password" 
        autocomplete="false"
        v-model="user.password"
        id="password" 
        placeholder="password" 
        class="login-input"
        required
        min="6"
        max="20"
      />
    </div>
    <div class="flex justify-center items-center">
      <button 
        class="bg-[#00AAA2] text-white text-lg md:text-3xl 
        py-2 px-4 rounded-md mt-4 md:mt-8"
      >
        Register
      </button>
    </div>
  </form>
</div>
</template>
<script>
export default {
  name: 'RegisterPage',
  data() {
    return {
      user: {
        username: '',
        password: '',
      }
    }
  },
   computed: {
    // login() {
    //   return this.$store.getters['loginUser/loginUser'] === this.user
    // }
    },
  methods: {
    // loginUser() {
    //   this.$store.dispatch('loginUser/loginUser', this.user)
    // }
  }
}
</script>

-- Darkmode<!-- <div>


